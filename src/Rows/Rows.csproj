<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Rows</AssemblyName>
    <RootNamespace>Rows</RootNamespace>
    <Version>0.2.1</Version>
    <Authors>GitMCP</Authors>
    <Description>Adds oars to boat seats so extra players help the ship go faster.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- See the Bicicreta project file for why Jotunn's build assets are excluded. -->
    <PackageReference Include="JotunnLib" Version="2.30.0" ExcludeAssets="build" />
  </ItemGroup>

</Project>
